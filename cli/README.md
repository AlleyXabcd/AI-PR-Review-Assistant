# CLI — AI PR Review 助手

基于 Python + Typer 的命令行工具，复用后端分析核心，直接在终端评审 GitHub PR。

计划用法：

```bash
pr-review <github-pr-url>          # 终端输出彩色评审报告
pr-review <github-pr-url> --json   # 输出结构化 JSON
```

> 实现随后续 PR 补充。
